const names = ['Tarek', 'Edie', 'Gouda', 'John'];
const colleagues = ['Ahmad', 'Tarek', 'Ayda', 'Ismail', 'Alex', 'Vaida', 'Yoss'];
console.log(names.length);
console.log(colleagues.length);

if (names.length === colleagues.length) {
    console.log('They are the same!');
} else {
    console.log('Two different sizes');
}